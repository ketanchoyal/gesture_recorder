name: gesture_recorder_devtools_extension
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  devtools_extensions: ^0.4.0
  devtools_app_shared: ^0.4.0
  dtd: ^4.0.0
  vm_service: ^15.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true